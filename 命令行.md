
### 生成正确ll
```bash
clang -target arm-linux-gnueabihf -mcpu=cortex-a8 -mfloat-abi=hard -x c++ -S -emit-llvm example.sy -o exampleOK.ll
```

### 测试命令
```bash
make test TEST_PATH=test/functional_test/Basic
```

### 应用dce优化（ O1包含死代码消除 ）
```bash
clang -target arm-linux-gnueabihf -mcpu=cortex-a8 -mfloat-abi=hard -x c++ -S -emit-llvm -O1 example.sy -o exampleOK.ll
```
