
### 生成正确ll
```bash
clang -target arm-linux-gnueabihf -mcpu=cortex-a8 -mfloat-abi=hard -x c++ -S -emit-llvm example.sy -o exampleOK.ll
```

### 测试命令
```bash
make test TEST_PATH=test/functional_test/Basic
```

### 仅应用dce优化
```bash
clang -target arm-linux-gnueabihf -mcpu=cortex-a8 -mfloat-abi=hard -x c++ -S -emit-llvm -O0 example.sy -o example_unoptimized.ll
llvm-as example_unoptimized.ll -o example_unoptimized.bc
opt -S -dce example_unoptimized.bc -o exampleOK.ll
```
