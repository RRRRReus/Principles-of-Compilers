# include"sysyruntimelibrary/sylib.h"

int sum(int n, int a[])
{
    int i=0;
    int s=0;
    while(i<n)
    {
        s=s+a[i];
        i=i+1;
    }
    return s;
}
int main()
{
     int a[10];
    int c=345;
    int b[5]={1,2,3,4,c};
    int i=0;

    putarray(5,b);
    while(i<5)
    {
        //return 15;
        //a[i]=i;
        //putint(a[0]);
        putch(63);
        putint(b[i]);
        putch(10);
        i=i+1;
    }

    //   int i=0;
    // // a[8]=54;
    // // // if(i<7)
    // // //    return 12;
    // while(i<7)
    // {
    //     //return 15;
    //     a[i]=i;
    //     //putint(a[0]);
    //     putch(63);
    //     putint(a[i]);
    //     putch(10);
    //     i=i+1;
    // }
    // while(i<7)
    // {
    //     //return 15;
    //     //a[i]=i;
    //     //putint(a[0]);
    //     putch(63);
    //     putint(a[i]);
    //     putch(10);
    //     i=i+1;
    // }

    // // putch(10);
    // // putint(a[3]);
    
    // putch(10);
    // putint(a[0]);
    // putch(10);
    // putint(a[1]);
    // putch(10);
    // putint(a[2]);
    // putch(10);
    // putint(a[3]);


    // a[1]=34;
    // a[2]=12;
    // //a[0];
    // //a[0];
    // putint(a[1]);
    // putch(10);
    // //putint(a[0]);

    // //putint(a[0]);
    // // a[0];
    // // a[0];
    // // a[0];
    // // putint(a[0]);putch(10);
    // // putint(a[0]);putch(10);
     return sum(5,b);
}

